<!-- will be yielded into /pages/show_page -->
<%= link_to(page.short_mime_type, original_path(page.id))%>
<%= (link_to "Delete", {:controller => 'upload_sorting', :id=>page.id, :action => 'destroy_page'}, {:remote => true, :confirm => 'Are you sure?'}) %>